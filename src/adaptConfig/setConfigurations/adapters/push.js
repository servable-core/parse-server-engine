// import _path from "path";
// import { fileURLToPath } from "url";
// import { dirname } from "path";
// const __filename = fileURLToPath(import.meta.url);
// const __dirname = dirname(__filename);

// const confPath = _path.resolve(__dirname, "../conf");

export default {
  /* Ios: [
     {
       token: {
         key: `${confPath}/AuthKey.p8`,
         keyId: process.env.SERVABLE_PUSH_IOS_KEYID,
         teamId: process.env.SERVABLE_PUSH_IOS_TEAMID
       },
       topic: process.env.SERVABLE_APP_BUNDLE_ID,
       production: false
     },
     {
       token: {
         key: `${confPath}/AuthKey.p8`,
         keyId: process.env.SERVABLE_PUSH_IOS_KEYID,
         teamId: process.env.SERVABLE_PUSH_IOS_TEAMID
       },
       topic: process.env.SERVABLE_APP_BUNDLE_ID,
       production: true
     }], */
  /*
android: {
  apiKey: process.env.SERVABLE_ANDROID_FCM_SERVER_KEY
}, */
}
